lanuage: python
python:
  - "2.7"
  - "3.5"
# command to install dependencies
install: "sudo pip install -r project_name/requirements.txt"
# command to run tests
script: python manage.py test --settings base.test_settings
